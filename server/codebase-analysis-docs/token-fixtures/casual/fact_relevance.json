{
  "system": "You are a strict relevance judge.\n\nFor every numbered pair of a message and entity facts, decide whether the\nmessage is meaningfully related to those facts.\n\nReturn exactly one judgment for every supplied index. Do not invent indexes.\nPrefer false when the relationship is weak, generic, or based only on a shared\nword.",
  "user": "For each index, determine if the message relates to the entity's facts.\n\n1. Message: \"I think source_events should be the durable record, not document chunks. The chunk is useful for extraction and citations, but it should not be the whole provenance model.\" | Facts: document chunks are extraction units, webhook ingestion uses idempotency keys, facts are active memory\n2. Message: \"Document chunks are extraction units, but the original file is the source. If the user asks where a fact came from, the answer should be something like Contract.pdf, page 4, chunk 12.\" | Facts: webhook ingestion uses idempotency keys, facts are active memory, sessions are process boundaries\n3. Message: \"Actually, do not add source ranking yet. Corrections should be conversational. If Stripe and a note disagree, I can just tell Knoggin what the active memory should be.\" | Facts: facts are active memory, sessions are process boundaries, source_events are durable observed inputs\n4. Message: \"Keep source types to message, doc, and webhook for now. Webhook is broad enough to cover provider callbacks, polling jobs, game moves, and automation events.\" | Facts: sessions are process boundaries, source_events are durable observed inputs, document chunks are extraction units\n5. Message: \"Webhook automation should create its own session so I can inspect the process later.\" | Facts: source_events are durable observed inputs, document chunks are extraction units, webhook ingestion uses idempotency keys\n6. Message: \"For PDFs, I want page and chunk provenance available even if the first UI only shows the file name.\" | Facts: document chunks are extraction units, webhook ingestion uses idempotency keys, facts are active memory\n7. Message: \"The profile job should consolidate less often but with richer context for daily users.\" | Facts: webhook ingestion uses idempotency keys, facts are active memory, sessions are process boundaries\n8. Message: \"Relationship extraction matters when I talk through project ownership and dependencies.\" | Facts: facts are active memory, sessions are process boundaries, source_events are durable observed inputs"
}
